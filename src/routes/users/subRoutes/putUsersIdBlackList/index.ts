// Check if user with id === req.params.id exist.
// check if blacklist with userId === user.id exist.
// check if blacklist is not expired.
// check if req.body.reason and req.body.time are valid.
// update blacklist according to req.body.
